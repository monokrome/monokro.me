extends layout

block prepend title
  | #{ page.title + " - "}

block content
  include author

  article.article
    header
      span.date Posted #{ moment(page.date).format('MMMM DD, YYYY') }

      h2=page.title

    section.content!=typogr(page.html).typogrify()

block prepend footer
  div.nav
    a(href='/') « back to blog
