extends layout

block prepend title
  | #{ page.title + " - "}

block content
  include author

  article.article
    header
      p.date
        span=moment(page.date).format('MMMM DD, YYYY')
      h1=page.title

    section.content!=typogr(page.html).typogrify()

block prepend footer
  div.nav
    a(href='/') « back to blog
