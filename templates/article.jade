extends layout

block prepend title
  | #{ page.title + " - "}

block content
  include author

  h1=page.title

  article.article
    section.content!=typogr(page.html).typogrify()

block prepend footer
  div.nav
    a(href='/') « back to blog
